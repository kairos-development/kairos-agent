// Package styles defines the TUI visual token palette.
//
// It maps the shared Kairos semantic tokens (mode, connectivity,
// license, risk, success, danger, warning, info, muted) to
// terminal-safe Lipgloss styles. Theme profiles (Nocturne Terminal,
// High Contrast, Monochrome) are defined here.
package styles
