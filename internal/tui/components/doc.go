// Package components contains pure rendering widgets for the TUI.
//
// Components receive viewmodel data and produce terminal output via
// Lipgloss styles. They must not import actions, query, commands,
// or any domain/service/provider packages. They depend only on
// viewmodel types and styles.
package components
