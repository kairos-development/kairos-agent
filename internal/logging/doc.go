// Package logging provides structured logging utilities.
//
// Logs include component, operation, correlation_id, and entity
// identifiers. Secrets are never logged. Error fields are wrapped
// separately from the message.
package logging
