// Package bybit adapts the Bybit exchange connector to the agent's
// domain connector interface.
//
// It converts between connector-layer models from kairos-contracts and
// domain entities from internal/domain/entity. This package is a
// provider: it must not import service, controller, or UI packages.
package bybit
