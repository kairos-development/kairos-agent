// Package tui provides TUI-specific controllers that bridge the
// agent service layer with the terminal interface.
//
// This is the adapter layer that converts between domain/service
// types and TUI-specific data transfer objects.
package tui
